# Agent 专家必背清单（速记手册）

> 依据《Agent 入门到实战》全文整理，按背诵优先级分级：**P0 = 必须脱口而出 / P1 = 必须熟练讲清 / P2 = 理解即可，被追问时能展开**。

---

## P0 级：核心必背（做到「不假思索」）

### 1. 一句话定义（专家第一句）

> **Agent = LLM（大脑）+ 工具生态（手脚）+ 记忆系统（经验沉淀）+ 规划引擎（策略制定）**

传统 LLM 给建议，Agent 做事情。Agent 具备 **目标拆解 → 资源调用 → 结果落地** 的全流程闭环能力。

### 2. 三大自主能力

| 能力 | 含义 |
|---|---|
| 自主感知 | 理解当前环境和任务需求 |
| 自主决策 | 制定执行计划并动态调整 |
| 自主执行 | 调用工具完成实际任务 |

### 3. ReAct 循环（全文之魂，必须能默写提示词骨架）

```
Question: 需要回答的问题
Thought:  思考该做什么
Action:   行动（必须是工具列表之一）
Action Input: 行动的输入
Observation:  行动的结果
...（Thought/Action/Action Input/Observation 可重复 N 次）
Thought: 我现在知道最终答案了
Final Answer: 最终答案
```

三个关键占位符（面试高频）：

- `{tools}` / `{tool_names}`：告诉 Agent 有什么工具可用
- **`{agent_scratchpad}`：携带历史思考轨迹，ReAct 的灵魂**——没有它，Agent 每轮都会失忆重想
- `max_iterations=5`：防死循环的保险丝

### 4. 四大核心模块一句话

| 模块 | 一句话 | 关键参数 |
|---|---|---|
| LLM 大脑 | 认知、推理、决策 | **temperature=0** 提高推理稳定性；提示词引导「分步思考」= CoT |
| 规划模块 | 任务拆解、路径统筹 | ReAct vs Plan-and-Execute |
| 记忆模块 | 留存上下文与经验 | 短期 Buffer / Summary，长期向量库 |
| 工具集 | 手脚与超能力 | Tool(name, func, **description**) |

### 5. 两种规划方法的选择标准（必背对比）

| | ReAct（边想边做） | Plan-and-Execute（先计划后执行） |
|---|---|---|
| 模式 | 思考→行动→观察循环 | 一次性全量规划 → 按计划执行 |
| 优点 | 灵活，可根据中间结果调整 | 高效，规划只调 1 次 LLM；可并行 |
| 缺点 | 每步都调 LLM，Token 消耗大 | 不灵活，难以中途调整 |
| 适用 | **探索性任务**（不知道中间会遇到什么） | **流程固定的标准化任务** |
| 复杂混合 | 两者结合 | |

---

## P1 级：熟练讲清（能做到「画出图 + 讲出为什么」）

### 6. 四层架构

感知层（耳朵眼睛：接收理解输入）→ 认知层（大脑：LLM+规划+推理）→ 执行层（手脚：工具集）→ 记忆层（短期+长期）。数据流转：多轮「思考-行动-观察」循环逐步逼近答案，而非一次性生成。

### 7. 记忆系统（三级火箭）

| 层级 | 技术 | 要点 |
|---|---|---|
| 短期·全文 | ConversationBufferMemory | 存原始对话；超 Token 限制 → 换摘要 |
| 短期·摘要 | ConversationSummaryMemory | 超限自动总结：5000 tokens → 500 tokens |
| 长期 | 向量库（Chroma + Embeddings） | **本质是 RAG**：存经验 → 相似检索 → 注入 prompt |

高级技巧两条：① 重要性评分（LLM 打分 ≥7 才存长期）；② 时效管理（记忆带 expiry 时间戳，检索时过滤过期项）。

### 8. 工具设计三要素 + 三大最佳实践

三要素：**清晰名称 / 详细描述 / 标准输入输出**。

最佳实践（必背口诀：**「描述准、错友好、留日志」**）：

1. 描述精确——写清「输入格式 / 输出格式 / 适用场景 / **不适用场景**」，这是 Agent 选工具的唯一依据
2. 错误友好——失败时返回「错误原因 + 建议」，帮 Agent 调整策略而不是崩溃
3. 使用日志——记录每次调用的输入/输出/耗时，便于排查

### 9. 五大难点速查表（专家身份的「肌肉记忆」）

| 难点 | 解法（每条至少说出 2 个） |
|---|---|
| 无限循环卡死 | max_iterations/max_execution_time 硬上限；提示词写明终止条件；智能终止（检测重复动作/无新信息） |
| 工具选择错误 | 改描述（写清不适用场景）；加使用示例；工具推荐系统 |
| 上下文溢出 | 摘要压缩（SummaryBufferMemory）；分层记忆（近3轮全量/中期10轮要点/长期向量）；动态加载工具 |
| 错误与鲁棒性 | 工具层重试（指数退避 1s→2s→4s）；Agent 层三级降级（完整流程→简化流程→LLM 直接回答）；监控告警（失败率>30% 报警） |
| 成本失控 | 模型分级（简单任务用便宜模型）；缓存命中；批处理（10 任务 2~3 次调用，省 70%）；预算硬限制 |

### 10. 多 Agent 三模式 + 三挑战

| 模式 | 结构 | 适合 |
|---|---|---|
| 层级 Hierarchical | 经理分派 → 各 worker 执行 → 整合 | 层次清晰的任务 |
| 平等 Collaborative | 圆桌对话、相互协商（AutoGen 群聊） | 需要多角度思考 |
| 流水线 Pipeline | 固定顺序，各管一段（研究→大纲→草稿→编辑→SEO） | 步骤明确的任务 |

三挑战：**通信开销**（用结构化消息，必要时才调 LLM）、**死锁**（超时 + fallback）、**结果冲突**（置信度加权投票 / 专家仲裁）。

### 11. 框架选择口诀

> **学习找 LC，协作用 AG，团队 CrewAI，快速原型 Dify，生产级 LangChain + 自定义**

- LangChain：生态最全、教程最多，入门首选
- AutoGen：专为多 Agent 对话设计（GroupChat + Manager）
- CrewAI：角色扮演（role/goal/backstory 三件套）最自然
- Dify：拖拽可视化，业务人员低代码
- 新趋势：生产环境正从旧版 AgentExecutor 迁移到 **LangGraph**（图状态机）

### 12. 三种理解视角（讲方案时切换用）

① 员工视角：Agent = 自主完成任务的员工（设计职责范围/输入输出/错误处理）；② 循环系统视角：感知→思考→决策→行动→观察的反馈闭环（优化循环次数、终止条件）；③ 大脑+工具视角：LLM 与工具箱的通信（扩能力=加工具，提效率=写好描述）。

### 13. 提示词工程三技巧

**Few-shot 给示例 / CoT 引导分步思考 / 角色扮演定人设**（❌「帮我写代码」✅「你是 10 年经验的资深 Python 工程师，代码清晰易读、注重性能、考虑边界」）。

---

## P2 级：理解即可（被追问时能展开）

### 14. 意图识别（补充章节精华）

- **三层结构**：文本理解（转结构化数据）→ 意图分类（判断想干什么）→ 槽位填充（提取参数）→ 槽位验证 → 缺槽位自动追问
- **三大技术路线**：

| 路线 | 技术 | 特点 |
|---|---|---|
| 规则 | 关键词词典匹配 | 简单可控，覆盖窄；置信度 0.9 是写死的假值 |
| 机器学习 | jieba 分词 + TF-IDF + 朴素贝叶斯 | 20 条数据即可跑通，轻量基线 |
| 深度学习 | BERT 微调（bert-base-chinese，lr=2e-5，3 epochs） | 上下文语义理解最强 |

- **置信度四档**（必背）：≥0.85 直接执行；0.6~0.85 二次确认「您是想 X 吗」；0.3~0.6 提供选项；<0.3 拒绝并请换说法
- 多意图：按连词（然后/接着/还有）拆句分别识别

### 15. 实战三范式

- **智能客服**：意图识别 + 情绪检测（负面先安抚）+ 查单/FAQ 工具 + 转人工兜底
- **代码生成 Agent**：**生成 → 测试 → 修复闭环**——正则提取代码块（非贪婪 `(.*?)`）→ subprocess 临时文件沙箱执行（timeout=5）→ stderr 回灌给 LLM 修复 → 最多 3 次 → 通过后加 docstring
- **数据分析 Agent**：读取 Excel → LLM 产出 JSON 分析计划 → 按步骤执行（describe/groupby/可视化）→ 生成报告

### 16. 旧版坑与安全（体现你「真跑过」）

- 旧 API 已过时：`ConversationBufferMemory`、`create_react_agent`（langchain.agents）→ 新版用 **langgraph / langchain_community**；`from langchain.agents import Plan, Execute` 是伪代码，实际不存在
- 安全红线：**eval/exec 直接执行输入有注入风险**；SQL 必须参数化（`cursor.execute(sql, (order_id,))` 的 `?` 写法），不能字符串拼接
- 健康度 SLO：成功率 ≥95% 且延迟 <2s = HEALTHY；≥80% = DEGRADED；否则 UNHEALTHY

---

## 7 天背诵计划

| 天 | 内容 | 检验标准 |
|---|---|---|
| D1 | P0 全部 | 脱口说出公式 + 三能力 + 默写 ReAct 骨架 |
| D2 | ReAct 提示词模板逐字默写 + scratchpad 原理 | 白板写出完整模板 |
| D3 | 规划对比 + 记忆三级火箭 | 讲清「什么时候用 P&E」 |
| D4 | 工具三要素 + 五大难点速查表 | 每个难点说出 2 个解法 |
| D5 | 多 Agent 三模式三挑战 + 框架口诀 | 画出三种模式的结构草图 |
| D6 | 意图识别三层三技术 + 置信度四档 | 讲清客服机器人完整链路 |
| D7 | 实战三范式 + 旧版坑 + 全文串讲 | 15 分钟不看书讲完全体系 |

**终极检验**：给人讲一遍「从用户提问到 Agent 交付结果，中间发生了什么」——能不看稿讲清 6 步数据流转（输入→规划→ReAct 循环×N→整合→存记忆→输出），你就已经是 Agent 专家了。
